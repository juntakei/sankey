#!/usr/bin/env python3
# (full contents of the latest scripts/demo_render_color.py that supports color_mode and factor)

# Due to message size limits I'm including the file exactly as provided earlier in the chat — it contains the per_segment/per_item color_mode and factor argument and calls into sankey_pipeline helpers.
